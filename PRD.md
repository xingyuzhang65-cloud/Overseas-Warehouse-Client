# 拦截（客户）取消拦截能力补充 PRD

## 1. 背景

拦截（客户）页面原有能力以查看、筛选、导出拦截任务为主。客户在提交拦截申请后，如果业务计划变化，需要在管理端审批前主动撤回申请。与此同时，后台审批拒绝与客户主动取消在业务含义、操作来源和原因字段上不同，需要在页面上清晰区分。

本次补充围绕“客户取消拦截”进行优化，目标是让客户可以批量取消待审批拦截申请，并在列表、详情、导出中区分“客户取消”和“管理端驳回”。

## 2. 目标

- 在拦截（客户）“待审批”页签提供“取消拦截”操作入口，位置在“导出”按钮左侧。
- 支持客户勾选多条待审批拦截申请后批量取消。
- 点击“取消拦截”后弹出确认弹窗，并要求填写取消原因。
- 将原“审批拒绝”中的结果拆分为客户取消和管理端驳回，处理状态分别展示为“取消”和“驳回”。
- “取消/驳回原因”和“处理状态”按页面状态控制展示范围，避免在无关页面干扰客户查看。
- 导出字段与页面展示规则保持一致。

## 3. 范围

本次范围包含拦截（客户）页面的列表、状态页签、批量操作、取消弹窗、详情展示和导出字段。

不包含管理端审批流程、仓库实际拦截流程、费用核销流程和后端接口改造。

## 4. 页面与入口

### 4.1 操作按钮

在拦截（客户）“待审批”页签的操作区展示“取消拦截”按钮，按钮位于“导出”按钮左侧。其他页签均不展示该按钮。

按钮用于批量取消当前页签中客户已勾选的待审批拦截申请。

### 4.2 可取消范围

仅支持取消“待审批”的拦截申请。

当客户未勾选任务时，点击“取消拦截”提示客户先勾选需要取消的申请。

其他页签仍可勾选任务用于导出，但不能发起取消。

提交取消前再次校验状态；若申请已不处于待审批状态，阻止提交并提示重新选择。

## 5. 取消拦截弹窗

### 5.1 弹窗触发

客户勾选一条或多条待审批拦截申请后，点击“取消拦截”，打开取消确认弹窗。

### 5.2 弹窗内容

弹窗展示以下信息：

- 标题：取消拦截
- 摘要：提示当前将取消选中的拦截申请数量
- 必填字段：取消原因
- 操作按钮：返回、确认取消拦截

### 5.3 字段校验

取消原因必填，不允许只输入空格。

取消原因最多支持 200 字。

客户未填写取消原因时，点击“确认取消拦截”需要提示客户填写原因，并保持弹窗打开。

### 5.4 提交结果

客户确认取消后，系统将选中的待审批拦截申请更新为客户取消状态，并记录同一个取消原因。

提交成功后关闭弹窗，清空勾选状态，并刷新列表。

## 6. 状态与字段定义

### 6.1 处理状态

处理状态是拦截任务的业务处理结果字段，展示客户当前可理解的最终或当前状态。

处理状态字段为“全部”状态页的专属字段，同时在“已取消/已驳回”状态页中展示，用于区分客户取消和管理端驳回。

处理状态取值包含：

- 待审批
- 拦截中
- 拦截成功
- 拦截失败
- 取消
- 驳回

### 6.2 客户取消

客户取消表示客户在管理端审批前主动撤回拦截申请。

客户取消后的拦截任务归入“已取消/已驳回”页签，处理状态展示为“取消”。

客户取消需要展示取消原因。

### 6.3 管理端驳回

管理端驳回表示后台或系统因业务条件不满足而拒绝本次拦截申请。

管理端驳回后的拦截任务归入“已取消/已驳回”页签，处理状态展示为“驳回”。

管理端驳回需要展示驳回原因。

### 6.4 已取消/已驳回页签

原“审批拒绝”页签在客户侧展示为“已取消/已驳回”。

该页签合并展示客户取消和管理端驳回两类任务，但必须通过“处理状态”和“取消/驳回原因”字段区分来源。

## 7. 列表展示规则

### 7.1 全部状态

在“全部”状态页中展示“取消/驳回原因”和“处理状态”字段，依次位于“备注”字段前。仅取消、驳回任务展示对应原因，其他任务显示“—”。

### 7.2 已取消/已驳回状态

在“已取消/已驳回”状态页中展示“取消/驳回原因”和“处理状态”字段。

字段顺序为：

1. 取消/驳回原因
2. 处理状态
3. 备注

### 7.3 其他状态

除“全部”和“已取消/已驳回”外，其他状态页不展示“取消/驳回原因”和“处理状态”字段。

## 8. 详情展示规则

拦截详情中展示处理状态。

当任务为客户取消时，详情展示“取消原因”。

当任务为管理端驳回时，详情展示“驳回原因”。

非客户取消、非管理端驳回的任务不展示取消原因或驳回原因。

## 9. 导出规则

导出字段需要与当前状态页的列表字段保持一致。

在“全部”状态页导出时，包含“取消/驳回原因”和“处理状态”字段，字段位置在“备注”前。

在“已取消/已驳回”状态页导出时，包含“取消/驳回原因”和“处理状态”字段，字段位置在“备注”前。

在其他状态页导出时，不包含“取消/驳回原因”和“处理状态”字段。

## 10. 验收标准

- 仅“待审批”页签展示“取消拦截”按钮，位置在“导出”按钮左侧；其他页签均不展示。
- 未勾选任务时点击“取消拦截”，系统提示先勾选任务。
- 其他页签可勾选任务并导出，但不能发起取消。
- 勾选多条待审批任务时，可以一次性提交取消。
- 取消原因为空或仅为空格时不能提交。
- 提交取消后，相关任务进入“已取消/已驳回”页签，处理状态为“取消”。
- 管理端驳回任务在“已取消/已驳回”页签中处理状态为“驳回”。
- “取消/驳回原因”在“全部”和“已取消/已驳回”页签展示；其他任务显示“—”。
- “处理状态”在“全部”和“已取消/已驳回”页签展示，并位于“备注”前；已完成任务按结果显示“拦截成功”或“拦截失败”。
- 其他状态页不展示“取消/驳回原因”和“处理状态”。
- 导出字段顺序和当前列表字段顺序一致。
